# practicaprueba
Crear los archivos respectivos.

Luego crear en la misma carpeta del dockerfile la imagen del contenedor:
# docker build -t '<nombre de la imagen>' .

Ejecutamos el contenedor:

# docker run -p '<puerto:puerto> <nombre de la imagen>'

Abrir el html para verificar funcionamiento.
